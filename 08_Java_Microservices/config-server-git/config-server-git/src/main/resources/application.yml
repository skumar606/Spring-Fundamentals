server:
  port: 8888
spring:
  cloud:
    config:
      server:
        git:
          uri: https://github.com/skumar606/spring-cloudcongif-tolls
          search-paths:
          - station*
          repos:
            perf:
              pattern: '*/perf'
              uri: https://github.com/skumar606/spring-cloud-config-tolls-perf
              search-paths:
              - station*

